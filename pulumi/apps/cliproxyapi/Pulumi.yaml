name: cliproxyapi
description: CLIProxyAPI OpenAI-compatible Codex proxy
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: kubernetes-python
