name: flink
description: Apache Flink Kubernetes Operator and session cluster
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: helm-kubernetes-python
